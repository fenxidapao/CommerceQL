"""LLM 精筛（07 §3.2 树中的 `refine.py`）—— **P0 未实现，如实登记**。

归属窗口：**W2B**。本文件是检索内**唯一**允许依赖 `app.llm` 的子模块
（.importlinter TODO 1 的 forbidden 契约将按此留白）。

## 为什么 P0 不实现（决策点 Q5，2026-09-16 用户采纳建议）

1. 07 全文对 refine **只有落位与依赖豁免**（§3.2 树 + 附-1 + .importlinter TODO），
   **没有任何行为定义**（无 §6.x 小节：触发条件、prompt 形态、输出契约都不存在）；
2. 阶段 2B 的 DoD（docs/08 §3.4）四条均不涉及 refine；
3. `app.llm`（3A 网关）尚未交付——先实现就是拿桩写，必然返工。

## 后续窗口的开工条件（交接给架构 / 3A 侧）

- 触发条件与输入输出契约由 07 补定义（缺了就登记 U-xx，不要在实现里发明）；
- LLMPort 可用后，本子模块实现时必须满足：N-19（prompt_version 必录）、
  N-12（出站白名单）、C-11（mode 如实回填——refine 若降级必须显式标注）。

**红线自查**：这不是"做了一半的功能"，是"上游未定义的功能"。占位不假装。
"""
